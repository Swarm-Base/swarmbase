{
  "name": "swarmbase-v4",
  "version": "4.0.0",
  "description": "SwarmBase v4 — Clean Points Accumulation Contract",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:opbnb": "hardhat run deploy.js --network opbnb",
    "deploy:bsc": "hardhat run scripts/deploy.js --network bsc",
    "deploy:testnet": "hardhat run deploy.js --network opbnbTestnet",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^4.9.0",
    "dotenv": "^16.0.3",
    "hardhat": "^2.14.0",
    "keccak256": "^1.0.6",
    "merkletreejs": "^0.6.0"
  }
}
